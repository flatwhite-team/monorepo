{
  "name": "@flatwhite-team/prisma",
  "version": "2.0.2",
  "publishConfig": {
    "@flatwhite-team:registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/flatwhite-team/monorepo.git",
    "directory": "packages/prisma"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "license": "UNLICENSED",
  "scope": "@flatwhite-team",
  "scripts": {
    "build": "yarn db:generate && tsup ./index.ts --format cjs,esm --dts",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate",
    "dev": "yarn run build -- --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^5.5.2"
  },
  "devDependencies": {
    "@swc/core": "^1.3.95",
    "prisma": "^5.5.2",
    "tsup": "^7.2.0"
  }
}
